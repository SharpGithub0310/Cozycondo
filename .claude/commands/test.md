1. Identify how tests are run in this project (check package.json, CLAUDE.md, or common test files)
2. Run the test suite
3. If any tests fail:
   - Explain what failed and why
   - Fix the failing tests or the code causing failures
   - Re-run to confirm the fix works

4. Report the final test results

If $ARGUMENTS is provided, only run tests matching that pattern.